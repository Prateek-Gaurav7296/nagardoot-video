/**
 * Optional bundled ONNX. After exporting `assets/models/pothole.onnx`, set:
 *   export const BUNDLE_MODEL = require('../assets/models/pothole.onnx');
 * and add the file (see assets/models/README.txt). Leave null to load only from disk paths.
 */
export const BUNDLE_MODEL = require('../assets/models/pothole.onnx');
